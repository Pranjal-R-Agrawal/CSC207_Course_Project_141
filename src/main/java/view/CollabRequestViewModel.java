package view;

import use_case.collab_request.interface_adapter.CollabRequestState;

public class CollabRequestViewModel {
    // Do I have this? I'm wondering because from how I am imagining this, once the user clicks the collab request button,
    // There would be a popup from the post and comment section saying something along the lines of "Collab Request sent!"
    // or something like that. And that would be added onto the View of that respective use case.
}
